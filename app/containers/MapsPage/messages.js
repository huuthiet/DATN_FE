/*
 * MapPage Messages
 *
 * This contains all the text for the MapPage container.
 */
import {
  defineMessages
} from 'react-intl';

export const scope = 'MapPage.containers';

export default defineMessages({
  Detail: {
    id: `${scope}.Detail`,
    defaultMessage: 'Xem chi tiết',
  },
  Cancel: {
    id: `${scope}.Cancel`,
    defaultMessage: 'Hủy',
  },
});
